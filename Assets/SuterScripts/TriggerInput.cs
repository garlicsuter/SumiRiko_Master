using UnityEngine;
using UnityEngine.InputSystem;
using UnityEngine.XR;
using UnityEngine.XR.OpenXR.Input;

public class TriggerInput : MonoBehaviour
{
    //private UnityEngine.InputDevice leftController;
    //private InputFeatureUsage<bool> triggerButton;

    //private void Start()
    //{
    //    leftController = InputDevices.GetDeviceAtXRNode(XRNode.LeftHand);
    //    triggerButton = CommonUsages.triggerButton;
    //}

    //private void Update()
    //{
    //    if (leftController.TryGetFeatureValue(triggerButton, out bool triggerPressed) && triggerPressed)
    //    {
    //        Debug.Log("Trigger button on left controller was pressed!");
    //    }
    //}
}
